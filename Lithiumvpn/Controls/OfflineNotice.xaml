<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Lithiumvpn.Controls.OfflineNotice"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid FlowDirection="{Binding FlowDirection, Source={StaticResource Loc}}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                    Spacing="14" MaxWidth="360" Padding="24,40">
            <FontIcon Glyph="&#xEB5E;" FontSize="44"
                      Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                      HorizontalAlignment="Center"/>
            <TextBlock x:Name="TitleBlock"
                       Text="{Binding [Offline_Title], Source={StaticResource Loc}}"
                       FontSize="16" FontWeight="SemiBold"
                       HorizontalAlignment="Center" TextAlignment="Center"
                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
            <TextBlock x:Name="BodyBlock"
                       Text="{Binding [Offline_Body], Source={StaticResource Loc}}"
                       FontSize="13" TextWrapping="Wrap" TextAlignment="Center"
                       HorizontalAlignment="Center"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

            <StackPanel Orientation="Horizontal" Spacing="8"
                        HorizontalAlignment="Center" Margin="0,4,0,0">
                <Button x:Name="ImportButton" Click="Import_Click"
                        Visibility="Collapsed"
                        Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1" CornerRadius="10" Padding="16,8">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE710;" FontSize="13"
                                  Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                        <TextBlock Text="{Binding [Configs_AddFromClipboard], Source={StaticResource Loc}}"
                                   FontSize="13"
                                   Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    </StackPanel>
                </Button>

                <Button x:Name="ReconnectButton" Click="Reconnect_Click"
                        Style="{ThemeResource AccentButtonStyle}"
                        CornerRadius="10" Padding="16,8">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <ProgressRing x:Name="BusyRing" Width="14" Height="14"
                                      IsActive="False" Visibility="Collapsed"
                                      Foreground="White"/>
                        <FontIcon x:Name="ReconnectIcon" Glyph="&#xE72C;" FontSize="13" Foreground="White"/>
                        <TextBlock Text="{Binding [Offline_Reconnect], Source={StaticResource Loc}}"
                                   FontSize="13" Foreground="White"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
